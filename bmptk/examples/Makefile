#############################################################################
# 
# Makefile for bmptk examples (generated by Doxygen)
#
# (c) Wouter van Ooijen (www.voti.nl) 2012
# 
#############################################################################

.phony: clean build

build:
		cd lines && make build
		cd draw && make build
		cd paint && make build
		cd align && make build
      
clean:
		cd lines && make clean
		cd draw && make clean
		cd paint && make clean
		cd align && make clean
